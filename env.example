# Frontend envs (used by Vite)
# Leave empty to use same-origin + Vite dev proxy for local development.
# Set to full URL (e.g., https://your-app.up.railway.app) to point directly.
VITE_API_URL=
# Set to "true" on test server to bypass the password screen (keeps code intact).
VITE_BYPASS_LOGIN=false
# Optional: role assigned to the seeded demo user when bypassing login.
VITE_BYPASS_LOGIN_ROLE=yönetici